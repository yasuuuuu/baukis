- @title = '顧客の新規登録'
%h1= @title

#generic-form
  = form_for @customer_form, as: 'form', url: :staff_customers do |f|
    = render 'form', f: f
    .buttons
      = f.submit '登録'
      = link_to 'キャンセル', :staff_customers
